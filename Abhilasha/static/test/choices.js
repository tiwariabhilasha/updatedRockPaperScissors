import print from '../js/compvscomp.js';

describe('compvscomp', () => {

	it('it should select two random choices', () => {
		/*Since these are random choices so may be this will fail and probablity of choice 1 and choice 2 as rock and paper is 1/6*/
		randomChoices.toEqual('rock','paper');
	});

});